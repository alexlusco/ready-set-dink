# Ready, Set, Dink! (PerfectMind EventParticipants URL Builder)

Static web app for GitHub Pages that:
- accepts a PerfectMind BookMe4LandingPages/Class URL
- fetches its HTML (via a CORS proxy)
- extracts eventId/locationId
- prints a BookMe4EventParticipants URL
